<head> 
<style>
table, td, th {  
  border: 1px solid #ddd;
  text-align: left;
}

table {
  border-collapse: collapse;
  width: 100%;
}

th, td {
  padding: 15px;
}

</style>

</head>
<body>

<div id="teshowi">

<li style="display: inline-block;"><%= image_tag @product.product_img.url(:product_index), class: "fotojashow"%></li>


<div class="col-md-8">
</div>
<li style="display: inline-block; margin-left: 5%; margin-bottom: 9%;">   <table>
<tr>
  <th>Emri</th>
  <th>Kategoria</th>
  <th>Kodi</th>
  <th>Ngjyra</th>
</tr>
<tr>
  <td><%= @product.title %></td>
  <td><%= @product.category.name %></td>
  <td><%= @product.kodi %></td>
  <td><%= @product.pershkrimi %> </td>
</tr>
</table>
</div>
</div>

<div class="butons">
<% if logged_in? %>
<% if @product.user_id == current_user.id %>   
<%= link_to 'Ndryshoni Produktin', edit_product_path(@product), class: "btn btn-secondary", class: "btn-custom" %>
<%= link_to 'Fshijeni Produktin', product_path(@product), method: :delete, data: {confirm: "A jeni i sigurt?"}, class: "btn-custom2" %> 
<% end %>
<% end %>
</div>   


</body>
