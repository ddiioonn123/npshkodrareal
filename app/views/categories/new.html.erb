
<div class="col-md-6 col-md-offset-3" style="color: black;">

<%= form_for(@category) do |f| %>

<%= f.label :name %>
<%= f.text_field :name, class: 'form-control' %>

<%= f.submit "Krijo kategorine", class: "btn btn-primary" %>
<% end %>
<% if !logged_in? %> 

 <% redirect_to home_path %> 
 <% end %>
</div>
